<!-- 架构设计 -->
<section class="relative py-20 lg:py-32 bg-white dark:bg-slate-900 overflow-hidden" id="structure">
  <!-- 极光背景装饰 -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <div class="absolute -top-[20%] -left-[10%] w-[50%] h-[50%] bg-blue-500/5 rounded-full blur-[120px]"></div>
    <div class="absolute top-[40%] -right-[10%] w-[40%] h-[40%] bg-indigo-500/5 rounded-full blur-[100px]"></div>
  </div>

  <div class="container-custom relative z-10">
    <!-- 头部区域 -->
    <div class="text-center mb-16 lg:mb-24" data-aos="fade-up">
      <div class="inline-flex items-center px-4 py-1.5 bg-blue-500/10 text-blue-600 dark:text-blue-400 text-sm font-medium rounded-full mb-6 border border-blue-500/20">
        架构设计
      </div>

      <h2 class="text-4xl md:text-5xl lg:text-6xl font-bold mb-6 tracking-tight text-slate-900 dark:text-white">
        全栈技术<span class="text-blue-600 dark:text-blue-400 mx-2 relative inline-block">
          <span class="relative z-10">架构解析</span>
          <span class="absolute bottom-2 left-0 right-0 h-3 bg-blue-500/20 -rotate-1 z-0"></span>
        </span>
      </h2>

      <p class="text-lg text-slate-500 dark:text-slate-400 max-w-2xl mx-auto">
        基于 Vue3、ThinkPHP、uni-app 等前沿技术栈构建，打造企业级高性能应用
      </p>
    </div>

    <!-- Bento Grid 布局 -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 lg:gap-8 w-full mb-24">
      <!-- 主要特性卡片 - 前端生态 -->
      <div
        class="md:col-span-2 relative group rounded-3xl bg-white dark:bg-slate-800 border border-gray-100 dark:border-slate-700 p-8 lg:p-10 overflow-hidden hover:shadow-2xl hover:shadow-blue-500/5 transition-all duration-500"
        data-aos="fade-up"
      >
        <div class="absolute top-0 right-0 w-64 h-64 bg-blue-500/5 rounded-full blur-3xl -translate-y-1/2 translate-x-1/2 group-hover:bg-blue-500/10 transition-colors duration-500"></div>

        <div class="relative z-10">
          <div class="w-12 h-12 rounded-2xl bg-blue-500/10 flex items-center justify-center mb-6 text-blue-600 dark:text-blue-400 group-hover:scale-110 transition-transform duration-500">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.75 17L9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
            </svg>
          </div>
          <h3 class="text-2xl font-bold mb-4 text-slate-900 dark:text-white">现代化前端生态</h3>
          <p class="text-slate-500 dark:text-slate-400 mb-8 max-w-lg leading-relaxed">
            融合管理后台(Vue3)与PC端(Nuxt.js)的双重优势，提供 CSR 与 SSR 双模式支持。全面采用 TypeScript 开发，确保代码质量与可维护性。
          </p>

          <div class="grid grid-cols-2 sm:grid-cols-3 gap-4">
            {['Vue 3', 'TypeScript', 'Nuxt.js', 'Vite', 'Element Plus', 'Pinia'].map((tag) => (
              <div class="px-4 py-2 rounded-lg bg-gray-50 dark:bg-slate-700/50 text-sm font-medium text-slate-700 dark:text-slate-300 border border-gray-100 dark:border-slate-600 text-center">
                {tag}
              </div>
            ))}
          </div>
        </div>
      </div>

      <!-- 右侧垂直卡片 - 后端架构 -->
      <div
        class="md:col-span-1 md:row-span-2 relative group rounded-3xl bg-gradient-to-b from-blue-50/50 to-white dark:from-slate-800 dark:to-slate-900 border border-gray-100 dark:border-slate-700 p-8 flex flex-col hover:shadow-xl transition-all duration-500"
        data-aos="fade-up"
        data-aos-delay="100"
      >
        <div class="w-12 h-12 rounded-2xl bg-blue-500/10 flex items-center justify-center mb-6 text-blue-600 dark:text-blue-400 group-hover:rotate-12 transition-transform duration-500">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 12h14M5 12a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v4a2 2 0 01-2 2M5 12a2 2 0 00-2 2v4a2 2 0 002 2h14a2 2 0 002-2v-4a2 2 0 00-2-2m-2-4h.01M17 16h.01"></path>
          </svg>
        </div>
        <h3 class="text-xl font-bold mb-3 text-slate-900 dark:text-white">企业级后端架构</h3>
        <p class="text-slate-500 dark:text-slate-400 mb-8 flex-grow">
          基于 ThinkPHP 构建的高性能服务端，集成 Docker 容器化部署与多租户架构设计。支持数据隔离、权限控制及自动化运维，满足企业级私有化部署需求。
        </p>
        <div class="relative h-40 mt-auto rounded-xl bg-white/50 dark:bg-slate-800/50 border border-gray-100 dark:border-slate-700 overflow-hidden">
          <div class="absolute inset-x-4 top-4 bottom-0 space-y-3 opacity-50">
            <div class="h-2 w-3/4 bg-blue-200 dark:bg-blue-900/50 rounded-full"></div>
            <div class="h-2 w-1/2 bg-blue-200 dark:bg-blue-900/50 rounded-full"></div>
            <div class="h-2 w-full bg-blue-200 dark:bg-blue-900/50 rounded-full"></div>
            <div class="h-2 w-2/3 bg-blue-200 dark:bg-blue-900/50 rounded-full"></div>
          </div>
          <!-- 扫描动画效果 -->
          <div class="absolute inset-0 bg-gradient-to-b from-transparent via-blue-500/5 to-transparent h-full w-full animate-scan"></div>
        </div>
      </div>

      <!-- 下方两个小卡片 -->
      <!-- 移动端 -->
      <div
        class="md:col-span-1 relative group rounded-3xl bg-white dark:bg-slate-800 border border-gray-100 dark:border-slate-700 p-8 hover:border-blue-200 dark:hover:border-blue-800 transition-colors duration-500"
        data-aos="fade-up"
        data-aos-delay="200"
      >
        <div class="w-12 h-12 rounded-2xl bg-orange-500/10 flex items-center justify-center mb-6 text-orange-500">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 18h.01M8 21h8a2 2 0 002-2V5a2 2 0 00-2-2H8a2 2 0 00-2 2v14a2 2 0 002 2z"></path>
          </svg>
        </div>
        <h3 class="text-lg font-bold mb-2 text-slate-900 dark:text-white">跨平台移动端</h3>
        <p class="text-slate-500 dark:text-slate-400 text-sm leading-relaxed">
          采用 uni-app 框架，实现一套代码多端运行，覆盖小程序、App与H5。
        </p>
      </div>

      <!-- 工程化 -->
      <div
        class="md:col-span-1 relative group rounded-3xl bg-white dark:bg-slate-800 border border-gray-100 dark:border-slate-700 p-8 hover:border-blue-200 dark:hover:border-blue-800 transition-colors duration-500"
        data-aos="fade-up"
        data-aos-delay="300"
      >
        <div class="w-12 h-12 rounded-2xl bg-green-500/10 flex items-center justify-center mb-6 text-green-500">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
        </div>
        <h3 class="text-lg font-bold mb-2 text-slate-900 dark:text-white">工程化标准</h3>
        <p class="text-slate-500 dark:text-slate-400 text-sm leading-relaxed">
          ESLint + Prettier 统一代码规范，TypeScript 全程护航，确保项目长期可维护。
        </p>
      </div>
    </div>

    <!-- 底部 CTA 区域 -->
    <div
      class="relative rounded-[2.5rem] overflow-hidden bg-blue-600 dark:bg-blue-700 text-white shadow-2xl shadow-blue-500/20"
      data-aos="fade-up"
    >
      <!-- 背景装饰 -->
      <div class="absolute inset-0 bg-[url('/grid-pattern.svg')] opacity-10"></div>
      <div class="absolute -right-20 -top-20 w-96 h-96 bg-white/10 rounded-full blur-3xl"></div>

      <div class="grid lg:grid-cols-2 gap-12 p-10 md:p-16 relative z-10 items-center">
        <div>
          <div class="inline-block px-4 py-1.5 bg-white/10 rounded-full text-sm font-medium mb-6 backdrop-blur-sm border border-white/20">
            Open Source & Commercial
          </div>
          <h3 class="text-3xl md:text-4xl font-bold mb-6">
            不仅是源码交付<br/>更是您的<span class="text-blue-200">技术合伙人</span>
          </h3>
          <div class="flex flex-wrap gap-4 mb-8">
            {["完整源码交付", "私有化部署", "终身技术支持"].map((item) => (
              <div class="flex items-center gap-2 bg-white/10 px-4 py-2 rounded-lg backdrop-blur-sm border border-white/10">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
                <span class="text-sm font-medium">{item}</span>
              </div>
            ))}
          </div>
          <button class="group inline-flex items-center gap-2 bg-white text-blue-600 px-6 py-3 rounded-xl font-semibold hover:bg-blue-50 transition-colors shadow-lg">
            立即咨询
            <svg class="w-4 h-4 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3"></path>
            </svg>
          </button>
        </div>

        <div class="hidden lg:block relative">
          <div class="aspect-video rounded-2xl bg-white/10 border border-white/20 backdrop-blur-md p-6 relative overflow-hidden shadow-inner">
            <!-- 模拟代码界面 -->
            <div class="flex gap-2 mb-4">
              <div class="w-3 h-3 rounded-full bg-red-400/80"></div>
              <div class="w-3 h-3 rounded-full bg-yellow-400/80"></div>
              <div class="w-3 h-3 rounded-full bg-green-400/80"></div>
            </div>
            <div class="space-y-3 font-mono text-sm opacity-90">
              <div class="flex gap-4">
                <span class="text-blue-300">use</span>
                <span class="text-white">App\Service\AIService;</span>
              </div>
              <div class="flex gap-4">
                <span class="text-purple-300">class</span>
                <span class="text-white">Handler</span>
                <span class="text-white">{</span>
              </div>
              <div class="flex gap-4 pl-4">
                <span class="text-purple-300">public function</span>
                <span class="text-yellow-300">chat</span>
                <span class="text-white">($message) {</span>
              </div>
              <div class="pl-8 text-slate-300">// 智能回复逻辑</div>
              <div class="pl-8 flex gap-2">
                <span class="text-purple-300">return</span>
                <span class="text-white">AIService::completion($message);</span>
              </div>
              <div class="text-white pl-4">}</div>
              <div class="text-white">}</div>
            </div>

            <!-- 浮动卡片装饰 -->
            <div class="absolute -bottom-4 -right-4 bg-white text-blue-600 p-4 rounded-xl shadow-lg animate-bounce-slow">
              <div class="text-xs font-bold uppercase tracking-wider opacity-50 mb-1">Code Quality</div>
              <div class="text-2xl font-bold">100%</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <style>
    @keyframes scan {
      0% { transform: translateY(-100%); }
      100% { transform: translateY(100%); }
    }
    .animate-scan {
      animation: scan 3s linear infinite;
    }
    @keyframes bounce-slow {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-10px); }
    }
    .animate-bounce-slow {
      animation: bounce-slow 3s ease-in-out infinite;
    }
  </style>
</section>
